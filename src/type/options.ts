import { SheetCell } from './sheet'

export type Options = {
    sheet?:Array<SheetCell>,
    cellWidth?:Number,
    cellHeight?:Number,
    width?:Number,
    height?:Number,
    col?:Number,
    row?:Number
}